@using Egharpay.Extensions
@using Egharpay.Helpers
<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                    <span>
                        <img alt="image" class="img-circle avatar-navigation " src="~/Images/Avatar.png" />
                    </span>
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="clear">
                            <span class="block m-t-xs">
                                <strong class="font-bold">Hi, User</strong>
                            </span> <span class="text-muted text-xs block">Admin <b class="caret"></b></span>
                        </span>
                    </a>
                    <ul class="dropdown-menu animated fadeInRight m-t-xs">
                        <li><a href="@Url.Action("Profile", "Personnel")">Profile</a></li>
                        @*<li><a href="@Url.Action("Contacts", "AppViews")">Contacts</a></li>*@
                        @*<li><a href="@Url.Action("Inbox", "Mailbox")">Mailbox</a></li>*@
                        <li class="divider"></li>
                        <li><a href="@Url.Action("Logout", "Account")">Logout</a></li>
                    </ul>
                </div>
            </li>

            <li class="@Html.IsSelected(controller: "Dashboards")">
                <a href="@Url.Action("Dashboard_1", "Dashboards")"><i class="fa fa-th-large"></i> <span class="nav-label" data-i18n="nav.dashboard">Dashboards</span> <span class="fa arrow"></span></a>
            </li>

            <li class="@Html.IsSelected(controller: "Mobile")">
                <a href="@Url.Action("Index", "Mobile")"><i class="fa fa-mobile"></i> <span class="nav-label" data-i18n="nav.mobile">Mobile</span> <span class="fa arrow"></span> </a>
            </li>

            @if (User.IsSuperUser())
            {
                <li class="@Html.IsSelected(controller: "Seller")">
                    <a href="@Url.Action("Index", "Seller")"><i class="fa fa-users"></i> <span class="nav-label" data-i18n="nav.seller">Seller</span> <span class="fa arrow"></span> </a>
                </li>
            }
            @if (User.IsSuperUserOrAdminOrSeller())
            {
                <li class="@Html.IsSelected(controller: "SellerMobile")">
                    <a href="@Url.Action("Index", "SellerMobile")"><i class="fa fa-mobile"></i> <span class="nav-label" data-i18n="nav.sellermobile">Seller Mobile</span> <span class="fa arrow"></span> </a>
                </li>
            }
            @if (User.IsSuperUser())
            {
                <li class="@Html.IsSelected(controller: "HomeBanner")">
                    <a href="@Url.Action("Index", "HomeBanner")"><i class="fa fa-th-large"></i> <span class="nav-label" data-i18n="nav.banner">HomeBanner</span> <span class="fa arrow"></span> </a>
                </li>
            }

            @if (User.IsSuperUserOrAdminOrSeller())
            {
                <li class="@Html.IsSelected(controller: "Order")">
                    <a href="@Url.Action("Index", "Order")"><i class="fa fa-cart-arrow-down"></i> <span class="nav-label" data-i18n="nav.order">Order</span> <span class="fa arrow"></span> </a>
                </li>
            }

            @if (User.IsSuperUser() || User.IsAdmin() || User.IsMobileRepairAdmin())
            {
                <li class="@Html.IsSelected(controller: "MobileRepair")">
                    <a href="@Url.Action("MobileRepairOrder", "MobileRepair")"><i class="fa fa-wrench"></i> <span class="nav-label" data-i18n="nav.mobilerepair">Mobile Repair</span> <span class="fa arrow"></span> </a>
                </li>
            }
            @if (User.IsSuperUser())
            {
                <li class="@Html.IsSelected(controller: "Personnel")">
                    <a href="@Url.Action("Index", "Personnel")"><i class="fa fa-user"></i> <span class="nav-label" data-i18n="nav.personnel">Personnel</span> <span class="fa arrow"></span> </a>
                </li>
            }
            @if (User.IsSuperUser())
            {
                <li class="@Html.IsSelected(controller: "Trend")">
                    <a href="@Url.Action("Index", "Trend")"><i class="fa fa-bars"></i> <span class="nav-label" data-i18n="nav.trend">Trend</span> <span class="fa arrow"></span> </a>
                </li>
            }
            @if (User.IsSuperAdmin())
            {
                <li class="@Html.IsSelected(controller: "Partner")">
                    <a href="@Url.Action("Index", "Partner")"><i class="fa fa-user"></i> <span class="nav-label" data-i18n="nav.partner">Partner</span> <span class="fa arrow"></span> </a>
                </li>
            }
        </ul>
    </div>
</nav>